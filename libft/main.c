#include "libft.h"

// void	ft_putnbr_base(int nbr, char *base)
// {
// 	int		i = 1;
// 	int		j = 0;
// 	int		baselen = 0;
// 	char	num[32];
// 	int 	int_array[] = {4, 8, 2};
// 			int_array[1];

// 	while (base[baselen] != 0)
// 		baselen++;
// 	while (i != 0 && nbr / i != 0 && j++ <= 32)
// 	{
// 		num[j - 1] = base[(nbr / i) % baselen];		
// 		i *= baselen;

		
// 	}
// 	while (j-- > 0 && nbr)
// 		write(1, &num[j], 1);
// }

int main()
{
	//ft_putnbr_base(4, "01");
	// int i = 0;
	// char *str = "Hello";
	// char *s = "hi";
	// void **ap;
	// ap[0] = str;
	// ap[1] = s;

	
	printf("Hallo");
	// char *result = 
	//ft_memdel(ap);
	
	//printf("%d", memcmp("Hellol", "Hellos", 6));
	// if (result == NULL)
	// {
	// 	write(1, "Not found", 9);
	// 	return 0;
	// }
		// while(result[i] || i < 5)
		// {
		// 	write(1, &result[i] + 50, 1);
		// 	i++;
		// }
		return 0;
}
